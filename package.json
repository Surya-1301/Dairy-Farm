{
  "name": "dairy-farm-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix 'Dairy Farm' run dev",
    "build": "npm --prefix 'Dairy Farm' ci --include=dev && npm --prefix 'Dairy Farm' run build",
    "start": "npm --prefix 'Dairy Farm' run preview"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  }
}
