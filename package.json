{
  "name": "dairy-farm-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix 'Dairy Farm' run dev",
    "build": "npm --prefix 'Dairy Farm' install && npm --prefix 'Dairy Farm' run build",
    "start": "npm --prefix 'Dairy Farm' install && npm --prefix 'Dairy Farm' run preview -- --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  }
}
